#Initialize packages
library(shiny); library(leaflet); library(ggmap);

#User Interface
ui <- navbarPage(
  
)




#Server
server <- function(input, output) {
  
  
}


# Run Application 
shinyApp(ui = ui, server = server)
